export const Button = {
  // style object for base or default style
  variants: {
    outline: {
      border: '2px solid',
      borderColor: 'primary',
      color: 'primary',
    },
    solid: {
      bg: 'primary',
      color: 'white',
    }
  }
};
